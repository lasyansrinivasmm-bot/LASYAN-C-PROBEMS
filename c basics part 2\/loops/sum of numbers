#include <stdio.h>
int main() {
    //Your Code goes here!
    int n,sum,rem;
    scanf("%d",&n);
    for(;n!=0; ){

        rem=n%10;
   sum =sum+rem;
        n=n/10;
    }
    printf("%d",sum);
    return 0;
}
