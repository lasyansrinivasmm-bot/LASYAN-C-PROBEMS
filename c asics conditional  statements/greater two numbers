#include <stdio.h>

int main() {
    int num1,num2;
    scanf("%d%d",&num1,&num2);
    if (num1>num2){
        printf("%d is the greater",num1);

    }
else if (num2>num1){
    printf("%dis the greater",num2);
}
    return 0;
}
