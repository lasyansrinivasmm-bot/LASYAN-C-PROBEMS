#include <stdio.h>
int main() {
    //Your Code goes here!
    int age;
    scanf("%d",&age);
    if(age>=18){
        printf("eligible to vote");

    }
    else {
        printf("not eligible");
    }
    return 0;
}
